"""
API URL routing for the audit module.

Routes will be registered here as endpoints are implemented.
"""

from django.urls import path

urlpatterns = [
    # Phase 1+ will add audit routes
    # path("audit/", include("apps.audit.api.views")),
]