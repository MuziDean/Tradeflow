"""
API URL routing for the IAM module.

Routes will be registered here as endpoints are implemented.
"""

from django.urls import path

urlpatterns = [
    # Phase 1+ will add IAM routes
    # path("auth/", include("apps.iam.api.views")),
]