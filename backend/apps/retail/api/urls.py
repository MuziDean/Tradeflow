"""
API URL routing for the retail module.

Routes will be registered here as endpoints are implemented.
"""

from django.urls import path

urlpatterns = [
    # Phase 1+ will add retail routes
    # path("sales/", include("apps.retail.api.views")),
]